import React, { Component, PropTypes } from 'react'
import { connect } from 'react-redux'

class SmartExample extends Component {
  static propTypes = {

  }

  render () {
    return (
      <div></div>
    )
  }
}

const mapStateToProps = state => {
  return {

  }
}

export default connect(mapStateToProps)(SmartExample)